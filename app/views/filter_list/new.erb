<% form_for :filter_list, url(:filter_list, :create, user_id: user_id), class: '' do |f| %>
	<%= partial "filter_list/form", :locals => { :f => f } %>
	<div class="form-actions">
	<%= f.submit '创建', class:'btn btn-primary' %>
	</div>
<% end %>